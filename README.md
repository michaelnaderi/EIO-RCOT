## Repository Structure and Usage Guide

This repository contains the necessary files and scripts to replicate the simulation results presented in the paper. To execute the models, please follow this three-step workflow in order:

### 1. Generate the Incidence Tensors
* **File:** `EIO-Defaultxml-Base-2026-02-02-Nogit.xml`
* **Action:** Run this architectural schema file using the latest version of the HFGT toolbox. 
* **Purpose:** This will process the colored Petri net and generate the positive and negative hetero-functional incidence tensors.

### 2. Extract the $M$ Matrix
* **File:** `MLPPos_MLPNeg_reader.py`
* **Action:** Run this Python script after the toolbox completes its run.
* **Purpose:** This script reads through the JSON output generated by the HFGT toolbox and extracts the $M$ matrix (the Engineering System Net incidence matrix) required for the optimization problem.

### 3. Run the Optimization Model
* **File:** `EIO_HFNMFC` (Julia)
* **Action:** Execute this Julia script within your Julia-based simulation environment (ensuring you have the `JuMP` and `HiGHS` packages installed).
* **Purpose:** This runs the internalized Rectangular Choice of Technology (RCOT) model within the Hetero-Functional Network Minimum Cost Flow (HFNMCF) formulation using the matrices generated in the previous steps.
